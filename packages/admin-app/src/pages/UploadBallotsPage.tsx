import { useState } from 'react';

export const ElectionListPage: React.FC = () => {
    const [elections, setElections] = useState([]);
    return <div>Upload Ballots</div>;
};

export default ElectionListPage;
